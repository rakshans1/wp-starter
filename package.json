{
  "name": "wp-starter",
  "version": "0.0.1",
  "author": "Rakshan",
  "description": "wp-starter",
  "scripts": {
    "init": "./install.sh",
    "prestart": "docker-compose -f docker-compose-dev.yml up -d",
    "start": "cd wp-content/themes/wp-starter-theme && npm start",
    "stop": "docker-compose -f docker-compose-dev.yml down",
    "build": "cd wp-content/themes/wp-starter-theme && gulp build"
  },
  "dependencies": {},
  "devDependencies": {}
}